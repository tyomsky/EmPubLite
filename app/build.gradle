apply plugin: 'com.android.application'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.2"

    defaultConfig {
        applicationId "com.tyomsky.empublite"
        versionCode 1
        versionName "1.0"
        targetSdkVersion 23
        minSdkVersion 16
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
